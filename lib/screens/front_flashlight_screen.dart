import 'package:flutter/material.dart';

class FrontFlashlightScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('전면 전광판 화면'),
    );
  }
}
