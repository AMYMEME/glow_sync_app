import 'package:flutter/material.dart';

class RearFlashlightScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('후면 전광판 화면'),
    );
  }
}
